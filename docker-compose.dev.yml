services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: dev-m1nav-app
    ports:
      - "3100:3000"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://m1nav_user:m1nav_dev_password@db:5432/m1nav_dev
      - REDIS_URL=redis://cache:6379
    depends_on:
      db:
        condition: service_healthy
      cache:
        condition: service_healthy
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    networks:
      - dev-m1nav-network

  db:
    image: postgres:16-alpine
    container_name: dev-m1nav-db
    ports:
      - "5480:5432"
    environment:
      POSTGRES_USER: m1nav_user
      POSTGRES_PASSWORD: m1nav_dev_password
      POSTGRES_DB: m1nav_dev
    volumes:
      - dev-m1nav-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U m1nav_user -d m1nav_dev"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - dev-m1nav-network

  cache:
    image: redis:7-alpine
    container_name: dev-m1nav-cache
    ports:
      - "6400:6379"
    volumes:
      - dev-m1nav-cache-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - dev-m1nav-network

volumes:
  dev-m1nav-postgres-data:
  dev-m1nav-cache-data:

networks:
  dev-m1nav-network:
    driver: bridge
